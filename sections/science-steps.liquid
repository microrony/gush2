{{ 'section-science-steps.css' | asset_url | stylesheet_tag }}

{% assign the_problem_blocks = section.blocks | where: 'type', 'the-problem-block' %}
{% assign reports_blocks = section.blocks | where: 'type', 'reports-block' %}


<div class="science-steps page-width">
	<h2 class="gush-font-h2">{{section.settings.title}}</h2>
	<h3 class="gush-font-h3">{{section.settings.description}}</h3>

	<div>
	{% for block in the_problem_blocks %}
    
      <div class="the-problem-block {% if forloop.last -%}
			last-block
	 {%- endif -%}" {{ block.shopify_attributes }}>
			<div class="content">
				<h2>{{block.settings.title}}</h2>
				<p>{{block.settings.description}}</p>
			</div>
			<div class="image">
        {{ block.settings.image | image_url: width: 1280 | image_tag }}
			</div>
      </div>
  {% endfor %}
	</div>
	
	
	<div class="reports-blocks">
	{% for block in reports_blocks %}
      <div class="reports-block {% if forloop.last -%}
			last-block
	 {%- endif -%}" {{ block.shopify_attributes }}>
				<span class="gush-font-p2">{{block.settings.title}}</span>
				<h3 class="gush-font-h3">{{block.settings.description}}</h3>
				<span class="gush-font-p2">{{block.settings.number}}</span>
			
      </div>
  {% endfor %}
	</div>
	
</div>


	{% schema %}
	{
		"name": "Science Steps",
		"tag": "section",
		"settings": [
		{
			"type": "text",
			"id": "title",
			"label": "Title"
			},
			{
			"type": "textarea",
			"id": "description",
			"label": "Description"
			}
		],
		"blocks":[
	{
	"name": "The Problem",
	"type": "the-problem-block",
	"settings": [
	{
	"type": "text",
	"id": "title",
	"label": "Title"
	},
	{
	"type": "textarea",
	"id": "description",
	"label": "Description"
	},
	{
	"type": "image_picker",
	"id": "image",
	"label": "Image"
}
]
	},
	{
		"name": "The Solution",
		"type": "the-solution-block",
		"settings": [
		{
		"type": "text",
		"id": "title",
		"label": "Title"
		},
		{
		"type": "textarea",
		"id": "description",
		"label": "Description"
		},
		{
		"type": "image_picker",
		"id": "image",
		"label": "Image"
	}
	]
		},
		{
			"name": "Reports",
			"type": "reports-block",
			"settings": [
			{
			"type": "text",
			"id": "title",
			"label": "Title"
			},
			{
			"type": "textarea",
			"id": "description",
			"label": "Description"
			},
			{
				"type": "text",
				"id": "number",
				"label": "Number"
				}
		]
			},
			{
				"name": "Accreditations",
				"type": "accreditations-block",
				"settings": [
				{
				"type": "text",
				"id": "title",
				"label": "Title"
				},
				{
				"type": "textarea",
				"id": "description",
				"label": "Description"
				},
				{
				"type": "image_picker",
				"id": "image",
				"label": "Image"
			}
			]
				}
	],
		"presets": [
		{
		"name": "Science Steps"
		}
		]
		}
	{% endschema %}